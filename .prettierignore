# Patterns with an internal slash are root-anchored, so nested build output
# needs an explicit ** prefix.
node_modules
**/node_modules
dist
.next
out
**/src-tauri/target
**/src-tauri/gen
**/src-tauri/icons

# Vendored font binaries and their upstream licence text.
packages/ui/fonts

# Authored prose: the client-facing source of truth. Do not let tooling reflow it.
docs/prd.md
docs/brand.md

pnpm-lock.yaml
*.svg

# Generated by apps/website/scripts/build-photos.mjs on every photo rebuild.
# Formatting it only means the next `pnpm --filter @rasko/website photos` run
# undoes the change and `format:check` starts failing again.
apps/website/content/photos.ts
