# Tauri and Next.js both resolve better with hoisted peer deps in a workspace.
auto-install-peers=true
strict-peer-dependencies=false
# Keep the store deterministic across machines.
resolution-mode=highest
